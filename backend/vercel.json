{
    "version": 2,
    "builds":[{
        "src":"./src/config/server.js",
        "use": "@vercel/node"
    }],
    "routes":[
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}